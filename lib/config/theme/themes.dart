import 'package:flutter/material.dart';

final ColorScheme darkModeScheme = ColorScheme.dark(
  primary: const Color.fromARGB(255, 9, 116, 247),
  secondary: const Color.fromARGB(255, 181, 226, 255),
);

final ColorScheme lightModeScheme = ColorScheme.light(
  primary: const Color.fromARGB(255, 220, 9, 108),
  secondary: const Color.fromARGB(240, 252, 166, 166),
);
